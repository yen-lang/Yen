func esperar_zero() {
    let x = input<int>("Digite um número (0 para sair):");
    while (x != 0) {
        print "Você digitou: " + x;
        x = input<int>("Digite outro número (0 para sair):");
    }
    print "Fim!";
}

esperar_zero();
